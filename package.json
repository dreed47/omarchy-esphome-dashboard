{
  "name": "omarchy-esphome-dashboard",
  "version": "0.4.0",
  "description": "Fleet status for your ESPHome devices in the Omarchy bar. Omarchy shell plugin.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-manifest.mjs",
    "check": "node --check deviceLogic.mjs && node --check haLogic.mjs && node --check lib/io.mjs && node --check bin/esphome-dashboard && node scripts/validate-manifest.mjs && node --test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dreed47/omarchy-esphome-dashboard.git"
  },
  "engines": {
    "node": ">=18"
  }
}
